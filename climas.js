// climas.js
export const climaConfig = {
    // Objeto de clima (día soleado) - se carga automáticamente al iniciar la escena
    dia_soleado: {
        file: "https://cdn.jsdelivr.net/gh/Archinime/ArchiPapu@main/dia_soleado.glb",
        // Puedes ajustar la posición, rotación o escala si es necesario
        position: [0, 5, -8],  // Ejemplo: colocado detrás de la habitación, un poco elevado
        scale: 1.5,
        rotation: [0, 0, 0]
    }
};